import { HasNotRoleDirective } from './has-not-role.directive';

describe('HasNotRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new HasNotRoleDirective();
    expect(directive).toBeTruthy();
  });
});
