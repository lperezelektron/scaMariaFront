import { HasNotPermissionDirective } from './has-not-permission.directive';

describe('HasNotPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasNotPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
